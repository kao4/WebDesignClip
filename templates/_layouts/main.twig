<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Webデザイン制作の参考になる海外のWebデザインギャラリー・クリップ集">

    <title>
      {% if pageTitle is defined %}{{ pageTitle }} - {% endif %}WebDesignClip
    </title>

    {# CSS #}
    <link rel="stylesheet" href="/src/css/reset.css" />
    <link rel="stylesheet" href="/src/css/style.css" />
  </head>

  <body class="{{ bodyClass is defined ? bodyClass : '' }}">
    {# ヘッダー #} 
    {% block header %} 
    {% include "_components/_header.twig" %}
    {% endblock %} 
    
    {# メインコンテンツ #}
    <main>
      {% block content %} 
      {% endblock %}
    </main>

    {# フッター #} 
    {% block footer %} 
    {% include "_components/_footer.twig" %}
    {%endblock %} 
    
    {# JavaScript #}
    <script src="/src/js/tab.js"></script>
  </body>
</html>
