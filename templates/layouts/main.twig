<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Webデザイン制作の参考になる海外のWebデザインギャラリー・クリップ集"
    />

    <title>
      {% if pageTitle is defined %}{{ pageTitle }} - {% endif %}WebDesignClip
    </title>

    {# CSS #}
    <link rel="stylesheet" href="/web/css/base.css" />
    <link rel="stylesheet" href="/web/css/style.css" />

    <!-- {# 追加のhead要素をブロックで定義 #}
     {% block head %}
     {% endblock %} -->
  </head>

  <body class="{{ bodyClass is defined ? bodyClass : '' }}">
    {# ヘッダー #} {% block header %} {% include "components/header.twig" %}
    {%endblock %} {# メインコンテンツ #}
    <main>{% block content %} {% endblock %}</main>

    {# フッター #} {% block footer %} {% include "components/footer.twig" %}
    {%endblock %} {# JavaScript #}
    <script src="/web/js/tab.js"></script>
  </body>
</html>
